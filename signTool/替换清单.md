# 热更新目录替换清单

## 🎯 目标
将 `xhupdate` 目录名改为 `ldupdate`

## 📋 需要修改的文件

### 1. **核心配置文件**

#### `version_generator.js` (第4行)
```javascript
// 修改前
const BASE_URL = "http://pku.qzhagy.com/xhupdate/";

// 修改后  
const BASE_URL = "http://pku.qzhagy.com/ldupdate/";
```

#### `assets/project.manifest`
```json
// 注意：这个文件不需要手动修改
// 执行 version_generator.js 后会自动生成新的 project.manifest
// 然后手动替换 assets/project.manifest 文件
```

### 2. **配置文件**

#### `assets/resources/Main/debug.json` (第6-7行)
```json
// 修改前
"https://xh.ajvisas.com/xhupdate/",
"https://xyqp.online/xhupdate/"

// 修改后
"https://xh.ajvisas.com/ldupdate/",
"https://xyqp.online/ldupdate/"
```

#### `assets/resources/Main/release.json` (第6行)
```json
// 修改前
"http://pku.qzhagy.com/newupdate/"

// 修改后
"http://pku.qzhagy.com/ldupdate/"
```

### 3. **游戏配置文件**

#### `assets/GameBase/GameConfig.js`
```javascript
// 第266行 - 修改前
static DefaultUrl = "http://pku.qzhagy.com/newconfig/release_first.json";

// 第266行 - 修改后
static DefaultUrl = "http://pku.qzhagy.com/ldconfig/release_first.json";

// 第1279行 - 修改前  
static ConfigUrl = "https://pku.qzhagy.com/";

// 第1279行 - 修改后
static ConfigUrl = "https://pku.qzhagy.com/";  // 这个不需要修改
```

#### `assets/Main/Script/SelectLink.js`
```javascript
// 第95行 - 修改前
"https://pku.qzhagy.com/xhconfig/release_first.json",

// 第95行 - 修改后
"https://pku.qzhagy.com/ldconfig/release_first.json",
```

### 4. **其他生成器文件**

#### `version_generator1.js` (第4行)
```javascript
// 修改前
const BASE_URL = "http://pku.qzhagy.com/update/";

// 修改后
const BASE_URL = "http://pku.qzhagy.com/ldupdate/";
```

#### `media_generator.js` (第4行)
```javascript
// 修改前
const BASE_URL = "https://pku.qzhagy.com/update_media/";

// 修改后
const BASE_URL = "https://pku.qzhagy.com/update_media/";  // 不需要修改
```

### 5. **媒体文件**

#### `media/Audio/*.json` 文件
```json
// 修改前
{"packageUrl":"https://pku.qzhagy.com/update_media/","remoteUrl":"https://pku.qzhagy.com/update_media/media16.json"}

// 修改后
{"packageUrl":"https://pku.qzhagy.com/update_media/","remoteUrl":"https://pku.qzhagy.com/update_media/media16.json"}  // 不需要修改
```

### 6. **工具文件**

#### `assets/Main/Script/utils.js` (第770行)
```javascript
// 修改前
"https://pku.qzhagy.com/update/"

// 修改后
"https://pku.qzhagy.com/ldupdate/"
```

## 🛠️ 使用方法

### 方法1：使用自动化脚本
```bash
./signTool/change_update_dir.sh ldupdate
```

### 方法2：手动替换
```bash
# 使用sed命令批量替换
find . -name "*.js" -o -name "*.json" -o -name "*.manifest" | xargs sed -i 's|/xhupdate/|/ldupdate/|g'
find . -name "*.js" -o -name "*.json" -o -name "*.manifest" | xargs sed -i 's|xhupdate/|ldupdate/|g'
```

## 🌐 服务器端配置

### 需要创建的目录结构
```
服务器根目录/
├── ldupdate/                    # 新的热更新目录
│   ├── project.manifest
│   ├── version.manifest
│   └── assets/
└── ldconfig/                    # 新的配置目录
    └── release_first.json
```

### 文件上传
1. 执行 `node version_generator.js` 生成新的热更新文件
2. 手动替换 `assets/project.manifest` 文件
3. 上传到服务器的 `ldupdate/` 目录
4. 将配置文件 `release_first.json` 上传到服务器的 `ldconfig/` 目录

## ⚠️ 注意事项

1. **备份重要**：修改前务必备份原始文件
2. **服务器同步**：确保服务器端也创建对应的目录
3. **测试验证**：修改后测试热更新功能是否正常
4. **配置文件**：注意配置目录也要相应修改
5. **版本管理**：执行 version_generator.js 后手动替换 project.manifest
6. **文件替换**：生成后需要手动替换 assets/project.manifest 文件

## 🔄 还原方法

如果需要还原到原始状态：
```bash
# 从备份恢复
cp backup_*/version_generator.js ./
cp backup_*/assets/project.manifest ./
# ... 恢复其他文件
```
